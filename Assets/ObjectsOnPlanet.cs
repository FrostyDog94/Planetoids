using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectsOnPlanet : MonoBehaviour
{
    public List<GameObject> objOnPlanet;

    // Start is called before the first frame update
    void Start()
    {
        objOnPlanet = new List<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
